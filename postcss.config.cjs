module.exports = {
  plugins: {
    // Tailwind's PostCSS plugin was moved to a separate package.
    // Install `@tailwindcss/postcss` and replace the entry below.
    // See the install instructions in the repository root.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
